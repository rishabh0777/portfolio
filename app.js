// function updateDimensions() {
//     var box = document.querySelectorAll("cards");
//     var screenWidth = window.innerWidth;

//     if (screenWidth <= 768) { // Adjust based on your desired breakpoint
//         gsap.to(box, {duration: 0.5, width: "50px", height: "50px"});
//     } else {
//         gsap.to(box, {duration: 0.5, width: "100px", height: "100px"});
//     }
// }

// // Initial call to set dimensions based on screen size
// updateDimensions();

// // Listen for window resize event to update dimensions
// window.addEventListener("resize", updateDimensions);